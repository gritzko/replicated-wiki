#   DIFF tickets: the `diff:` view

`be diff:<uri>` renders the change between versions as a unified/token diff. Tickets here close gaps in what the view shows — submodule recursion, gitlink rendering, scoping. Each entry is one active ticket; it drops off once the whole ticket lands. The fix-and-land method is [Issues].

##  Active tickets

 -  [DIFF-003] — file-scoped `diff:<file>#L<n>` should render the WHOLE file
    with changes highlighted in place (vs tree/dir `diff:` = changed hunks only).

DIFF-001 (gitlink line + submodule content recursion) fully landed
(`ef0430da` part-a, `018a59c4` part-b). DIFF-002 landed earlier.

[DIFF-003]: DIFF-003.mkd "file-scoped diff: shows the whole file, changes hili'd"
[Issues]: ../../rule/Issues.mkd "the parallel fix-and-land loop"
