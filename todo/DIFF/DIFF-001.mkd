#   DIFF-001: `be diff:` must recurse into submodules and render gitlink/pin changes

`be diff:?<sha>` on a commit whose only change is a **submodule pin bump** shows NOTHING. A pin-bump commit's sole change is the sub's gitlink pointer (old-pin → new-pin), which the diff renderer does not emit, and `diff:` never recurses into the submodule to show what actually changed there — so the real work is invisible from the parent. Repro (in a worktree): `be diff:?29df3828` (the ABC-002 pin bump, abc `b8ed6277` → `d8656f41`) → empty output, even though the sub moved by a whole commit. `diff:` should render the gitlink change AND recurse into the sub to show its commit-range diff, the way [GET]/[POST]/[PATCH] already recurse. See [Verbs], [Submodules], [CLAUDE].

##  Issues
A gitlink-only commit diffs to nothing, and the sub's content is never reached.

 -  `be diff:?<sha>` on a pure pin-bump commit emits no output — the `<sub>` gitlink change (old pin → new pin) is not rendered as a diff line.
 -  `diff:` does not recurse into the submodule, so the actual changes between the old and new pins (the sub's commit-range diff) are never shown.
 -  Consequence: a parent commit that advances a sub by real work looks empty; review/audit of a pin bump is impossible from the parent.
 -  Surfaced verifying ABC-002 (`29df3828` bumps abc `b8ed6277`→`d8656f41`; the FILEErr change lives in the sub, invisible from `be diff:`).

##  Blockers
None. Localized to the `diff:` renderer + the submodule-recursion the other verbs already do.

##  Planned
Render the gitlink change and recurse into the sub.

 -  **Part-a LANDED `ef0430da`.** Repro-first ([CLAUDE] §17): a test asserting `be diff:` on a pin-bump commit (a) emits a gitlink line `<sub> <old-pin>..<new-pin>` ✓ (`test/diff/08-sub-pin-bump-gitlink`); (b) shows the sub's content diff for that range — **still open**.
 -  **DONE (part-a):** render a submodule gitlink change as a diff entry (old-pin → new-pin) — `diffref_collect_visit` now collects `WALK_KIND_SUB` entries (`is_sub`) and emits `<path> <old40>..<new40>` instead of dropping them; validated vs the real ABC-002 bump (`be diff:?29df3828` → `abc b8ed6277..d8656f41`).
 -  Recurse `diff:` into the mounted submodule and emit the sub's diff between old and new pins (path-prefixed under the mount), mirroring `GET`/`POST`/`PATCH` sub recursion; honor `--nosub` to suppress.
 -  **Part-b DEFERRED (design fork, 2026-06-11).** The existing `BEProjectorSubs` fan-out re-runs the identical `diff:?<TIP>` in each sub, which has no `<TIP>` commit (emits the gitlink line then `KEEPNONE`); the content needs `diff:?<old-pin>#<new-pin>`, range data only graf (`DIFFREF.c`) computes. Two designs surfaced: **(A) graf-driven** — `diffref_emit_gitlink` recurses into the sub's keeper shard in-process, but graf has no mount discovery (that is `be`'s job), breaking graf's per-store boundary; **(B) be-driven relay** — graf signals the pins to `be`, which rewrites the child URI to `diff:?<old>#<new>` and relays via `BERelaySub` (reuses the [[DIFF-002]] channel fix), fitting the boundary at the cost of a new graf→be pin-range signal. Decision pending.
 -  Keep a non-sub `be diff:` unchanged.

[Verbs]: ../../wiki/Verbs.mkd "the verbs hub"
[Submodules]: ../../wiki/Submodules.mkd "submodule recursion"
[GET]: ../../wiki/GET.mkd "GET verb"
[POST]: ../../wiki/POST.mkd "POST verb"
[PATCH]: ../../wiki/PATCH.mkd "PATCH verb"
[CLAUDE]: ../../CLAUDE.md "coding guidelines"
