#   URI tickets: branch/commit naming

Tickets that unify Beagle's branch-vs-commit-hash disambiguation behind one keeper-owned, branch-first, hash-length-agnostic rule (origin in [DIS-025], grammar in [URI-spec]). Each entry below is one ticket with its live state; an entry drops off once the whole ticket closes. The fix-and-land method is [Issues].

##  Active tickets

(none — URI-001 landed)

URI-001 (unify branch/commit naming: St.1 branch-first + St.2 length-agnostic full-sha
+ St.3 scope-only canonical form + St.4-5 retire-guessers/remote-delegation) DONE — all
5 stages landed/confirmed. Topic closed.

[URI-001]: URI-001.mkd "unify branch/commit naming machinery"
[URI-spec]: ../../wiki/URI.mkd "URI grammar + ref shapes"
[DIS-025]: ../DIS/DIS-025.mkd "squash merge-base / resolver-funnel origin"
[Issues]: ../../rule/Issues.mkd "the parallel fix-and-land loop"
