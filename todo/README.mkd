#   Active ticket board

Index of Beagle's open work: one topic per dir, each holding numbered [Plan]-shaped tickets cleared by the parallel [Issues] fix-and-land loop. Each line below names a topic and its live state; per-topic detail lives in `todo/<TOPIC>/README.mkd`. A fully-landed ticket drops from its topic page; a closed topic drops from here. Landed history lives in `be log`.

##  Active topics

 -  [BE] — dispatcher CLI front-end; open: BE-001 (red over-applied), BE-003 (uniform error line, needs a ruling).
 -  [URI] — branch/commit naming; URI-001 St.4-5 open (retire the guessers, remote delegation).
 -  [DIS] — cross-cutting spec/impl discrepancies; 22 open (store layout, bang-model specs, weave/patch base-position bugs).
 -  [MEM] — memory-safety audit (digest `MEM/MEM.mkd`); 16 open, several `js` ones blocked on a JSC build.
 -  [KEEP] — keeper object pool / walks / wire; open: KEEP-001 (converge the 3 tree-walkers), KEEP-002 (torn-index publish).
 -  [SUBS] — submodule recursion; open: SUBS-013 (`git submodule add` parity), SUBS-014/015 (put/blob edge cases).
 -  [POST] — commit-path gaps; open: POST-006 (POSTCFLCT false-fires on marker strings → [[DIS-040]]).
 -  [GET] — checkout / fetch / sync; open: GET-009 (be:// sync umbrella), GET-017 (→ [[DIS-039]]), GET-018 (atomic wt GET), GET-020 (incomplete checkout + SNIFFFAIL).
 -  [BLAME] — blame perf + output; open: BLAME-001, BLAME-003 (needs a ruling). BLAME-005 (per-commit hunks) landed `d2a7219c`.

[Plan]: ../wiki/Plan.mkd "the live planning page"
[Issues]: ../skill/Issues.mkd "the parallel fix-and-land loop"
[BE]: BE/README.mkd "dispatcher CLI front-end tickets"
[URI]: URI/README.mkd "branch/commit naming tickets"
[DIS]: DIS/README.mkd "cross-cutting spec/impl discrepancy tickets"
[MEM]: MEM/README.mkd "memory-safety audit tickets"
[KEEP]: KEEP/README.mkd "keeper object-pool / walks / wire tickets"
[SUBS]: SUBS/README.mkd "submodule-recursion tickets"
[POST]: POST/README.mkd "commit / fast-forward / push tickets"
[GET]: GET/README.mkd "checkout / fetch / view / search tickets"
[BLAME]: BLAME/README.mkd "blame-performance tickets"
