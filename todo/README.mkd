#   Active ticket board

Index of Beagle's open work: one topic per dir, each holding numbered [Plan]-shaped tickets cleared by the parallel [Issues] fix-and-land loop. Each line below names a topic and its live state; per-topic detail lives in `todo/<TOPIC>/README.mkd`. A fully-landed ticket drops from its topic page; a closed topic drops from here.

##  Active topics

 -  [ABC] — core library primitives (slices/buffers/net/poll/curl); ABC-001 **LANDED** (abc `37f85ddb`/parent `262e7e9e`: the libcurl SEGV was abc's own POL timer UAF feeding a wild fd to `curl_multi_socket_action` — re-resolve timer slot by stable identity, dedup timers, bounded CONNECTTIMEOUT, hermetic `CURLfw` test; MEM-010 still open); ABC-002 open (unify C errno→ok64 — `FILEerrno` switch vs `FILE_ERR_VOCAB` table return different codes for the same errno + omit EEXIST; delegate `FILEErr`→`FILEerrno`, delete the table).
 -  [ULOG] — append-log & store durability; ULOG-001 **LANDED `9cfa9639`** (root cause: `be` amplified a torn ULOG — a NUL at byte 0 → scan reads empty → `ULOGClose`/`ftruncate(0)` destroyed surviving bytes; wtlog+refs are both ULOGs, `.keeper` packs off-path so objects survived; fixed via `ULOGTORN` refusal + size-restore, regression added; initial-NUL trigger unpinned). Open follow-up candidates: clone-of-empty-refs writes 0-byte `refs` + `be get` HOME-escape; latent `O_TRUNC` seed in `sniff/SUBS.c`.
 -  [URI] — one keeper rule for branch/commit naming; URI-001 St.1-3 landed (branch-first, length-agnostic, scope-only canonical), St.4-5 pending; URI-002 **LANDED `230b5cbe`** (unify bang-factor handling — `DOG_BANG` bit byte + one `dog/` debanger every `!`-reader funnels through, abc pristine; `be` re-emits via `DOGDebangFeed` on canonicalize), URI-003 open (reframed → URI serialization **safety**: `URIutf8FeedSafe` round-trips feed→parse and errs out `URIUNSAFE` on any field mismatch, blocking cross-component injection from untrusted path/host/branch names; subsumes the pre-existing leading-`/` round-trip bug `ht/h/p#abc`→`/ht/h/p`) — **LANDED** (abc `fe7f86c2`/parent `cc4cc005`): `URIutf8FeedSafe` detect-and-err-out (`URIUNSAFE`) since the parser is non-decoding (Feed stays verbatim, no per-component escaping); leading-`/` recompose fixed; follow-up = a separate percent-encoding feeder `URIutf8FeedEscaped` + decoding parse mode; URI-004 **LANDED `97fa76e8`** (abc `6bf70732` on `d8656f41`/ABC-002 preserved: dropped the `uri` path-`segments` buffer → `abc/PATH`, dropped the Bx `uri` containers `urib`/`uris` → `u8csb`/`u8css` of unparsed text parsed on demand via `CLIUriParse`; adapts 17 CLI/exe files; `data` field + component slices stay).
 -  DIS — cross-cutting spec/impl discrepancies; DIS-025/002 fixed, DIS-001/024 (store layout), DIS-029 **LANDED** (MARK bare-indent `<div>` rendering — `mark_reconcile_divs` div-depth + leaf-boundary), DIS-030+031 landed (`bbefb693`: bang-model patch/post — `!`=whole-branch/forget, `post!`=--force, ban msgs ending in `!`), DIS-032 (specs to bang model, edited on disk), DIS-033 **LANDED `479fcc96`** (stale/false-fresh `.refs.idx`/`.wtlog.idx` → silent-empty `be log:`; `ulog_idx_spans_log` freshness guard); DIS-034 **LANDED `57756047`** (`be put <dir>` slashless dir lstat-probe recurses), DIS-035 **LANDED `4e8f5770`** (shed the PATCH whole-branch query-bang before project/ref derivation); DIS-036 (an unreachable / non-beagle / non-git remote fails opaque `WIRECLFL` instead of a clear diagnosis), DIS-037 (remote `~/.be` addresses projects by store-DIR name `dogs`/`abc`, not project TITLE `beagle`/`libabc`), DIS-038 ancestor-walk fallback **LANDED `1e4557cb`** (`DAGAncestorsTunable` falls back to the keeper commit-body on a parent-edge DAG miss, like `graf log` — `be head` ahead-count now true; test/head/24); the tip-resolver/ff-policy halves (GET-014 clobber, POST-015 silent-drop) remain — fetched-but-not-DAG-indexed peer tip → `DAGAncestors` bottoms out at the tip → `be head` bogus huge ahead-count + `be patch ?br!` absorbs nothing; LCA/ancestor walk lacks the keeper-commit-body fallback `graf log` already has) open; DIS-026/027/028 landed (`fbfea176`/`262d8922`/`465703db`: push cur→git-remote branch, relay peer push errors, `?main` resolves peer's refs/heads/main).
 -  MEM — memory-safety audit; 12 of the first 36 landed 2026-06-07, js 007/032/033 blocked on JSC; a 2026-06-11 error/unexpected-path sweep added MEM-037..045 (1 high, mediums, lows): MEM-037 abc/FILE.c FILEMapFD/FILEBookFD fd-leak-on-error (root cause), MEM-038 **HIGH** woof POL callbacks have no call() frame → HUNK BASS toks-carve never rewound → unbounded server leak (abc/POL + woof + dog/HUNK), MEM-039 js io.mmap SIZE_MAX overrun + mmap leak, MEM-040 graf 18MB own-open leak on `be head`/`be map` errors, MEM-041 sniff PATCH/POST error-path leaks, MEM-042 keeper REFSResolve ULOG leak, MEM-043 dog DOGPup overflow leaks, MEM-044 spot LSM leaks, MEM-045 bro BROOpenFile leak; digest in `MEM/MEM.mkd`. LANDED: MEM-037 (abc `e2ed7956`/parent `22dbd17b`), MEM-045 `674f555e`, MEM-038 **HIGH** `a296d54c`, MEM-043 `d17616fc`, MEM-040 `d857a9cc`, MEM-044 `91f215a4`, MEM-042 `6d7ebd5a`, MEM-041 `a338e631`; open: MEM-039 (js, latent + CI-unbuildable); MEM-041/042/044 landed 2026-06-11 via fresh-clone salvage (in-place worktree FF blocked by [[GET-016]]); follow-ups: abc/LEX.c fd-leak, dog/HUNK.c chained-alloc OOM.
 -  SUBS — submodule recursion; SUBS-001..012 landed, SUBS-013/015 open, SUBS-017 open (parent `be post be://` recursion can't push a sub to its remote project — `WIRECLFL` though the project exists), SUBS-018 **LANDED `b3154948`** (`BEGetDrainSubs` takes a `scope_path` and skips subs a path-scoped `be get <file>` does not reach — the mount itself or a path under it; empty scope = whole-tree = every sub; only a LOCAL bareword path is a scope, a transport URI's path is the clone source; test/get/47), SUBS-016 **LANDED** (abc `8b9dfe0c`/parent `109c7467`: guard the sub-seed on `FILEExists` so a live `refs`/`wtlog` is never `O_TRUNC`-wiped; revitalized `FILEExists` as `ok64`, purged the regex-migrated `FILENOENT`→canonical `FILENONE`, `FILE_ERR_VOCAB` stopgap pending ABC-002); digest in `SUBS/SUBS.mkd`.
 -  [POST] — commit-path gaps; POST-003 landed (`50b8c468`: recursive POST enumerates freshly-added subs, `test/post/15`), POST-004 landed (`50b8c468`: PATCH detached-sub recursion, `test/post/17`), POST-005 landed (dogs merge: untracked mtime-alias not swept, `test/post/28`), POST-002 landed (`2b6026af`: `#`-led msg body → fragment verbatim in `DOGNormalizeArg`, grammar untouched/RFC 3986, `test/post/34`); POST-010 landed (`03355e9b`: swallow benign EPIPE on a no-op `file://` re-push, peer-side `RECVEmitResponse` n==0, `test/post/35`), POST-011 part A landed (`a6129afc`: noop `be patch` stages no patch row, `be-patch-noop`; part B = clearing staged rows → [GET]-008 `be get!`), POST-012 landed (`5686c2b3`: suppress no-op "nothing to promote" line in `-q`/`--tlv` sub recursion, `test/post/36`), POST-007 landed (`06730990`: `be://` msg/scheme roundtrip regression tests — underlying bug already fixed by POST-002; `test/post/37-msg-scheme-roundtrip` + `dog/test` DOG case), POST-013 landed (`5de65a1d`: synthetic `?.beagle/` sub defaults to the git remote's advertised HEAD/main/master, `test/post/37-sub-synth-git-default-branch`); open: POST-006 (`POSTCFLCT` false-fires on comment markers — resolution: detect WEAVE shape else `be post!`), POST-014 (`be post be://` lands the push but strands the peer wt — `be get ?` wt-advance returns 157 — and exits `SNIFFFAIL`), POST-015 spam half **LANDED `fefaf7e4`** (keeper tree walk skips a `.be` entry silently instead of one `walk: bad path '.be'` line per scanned tree; genuinely-bad names still warn; test/get/48); silent-drop half open (absorbed commit collapses to the existing tip — blocked on [[DIS-038]] reachability); POST-008 landed (`ebde9097`: host-less push routable-target gate), POST-009 landed (`563d7a16`: strip `?/proj` selector before push branch), POST-001 landed (`eabf0c6e`/`8254ee85`: `--nosub` honored + scheme-gated recursion).
 -  [PUT] — staging gaps; PUT-001 landed (`d628fd67`: `be put <sub>/<path>` relays into a mounted sub via `BEActSubsPut`/`BERelaySub`, wtlog-only, `test/put/12`). No open tickets; DIS-034 (`be put <dir>` slashless) **landed `57756047`** — see DIS.
 -  [GET] — checkout gaps; GET-001 landed (`3251cb48`: weave-merge BASS BNOROOM), GET-002 landed (`0f7d06b2`: bare-authority URI completion from history), GET-003 landed (`a70e34cd`: `be://host?/proj` empty-path clone past WIRECLFL), GET-005 landed (`b5771abc`: `SNOROOM` large-pack wire ingest), GET-006 landed (`64e42597`: `be get` ancestor-wtlog escape; re-target c->repo at cwd, test/get/40), GET-007 landed (`5124b2ee`: `be://` re-fetch shard bloat — `wire_locate_sha` latest-copy so the watermark stops re-shipping duplicate tails; `WIREREFETCH`/`WIREE2EREFETCH`; deeper reachability rewrite → GET-009); GET-008 landed (`23ad85b6`: verified — `be get! '?'` already clears a poisoned wtlog via its appended `get`-row boundary, append-only, no truncation; test/get/41); GET-004 landed (`c47623fe`+`fc322bcf`: `<root>/.be` store composition consolidated into `HOMEBeDir`/`HOMEMakeBeDir`, RO never mkdirs strays, `.be`-as-a-name leak plugged, upload-pack fast-fails an absent store), GET-010 landed (`c47623fe`: refuse empty-source clone + `HOME`-bound shard discovery, test/get/42-43), GET-011 landed (`b680eb3a`: submodules fetch from the in-flight `be get` source first via `subs_candidate_from_source`, `.gitmodules` last — local/offline clones work); GET-012 landed (`4ea59dc1`: `be get file://<store>?/proj` attaches a store-backed worktree, not a throwaway clone — network rows gate on a real HOST not the `//` sigil, and the cwd `.be` shape disambiguates same-URL intent: absent→worktree, dir→clone, file→switch; `wt_attached` suppresses keeper/spot/graf get; test/get/46); GET-016 part 2 landed (`52ba21b5`: `be get --force` full tree reset — `force_orphan_cb` unlinks wt-only tracked orphans, untracked survive; part 1 atomicity split → GET-018); open: GET-009 (efficient `be://` sync umbrella: reachability upload-pack + cross-file serving + redundant-pack GC; St.1 landed via GET-007), GET-017 (switch-shaped store-worktree `be get --nosub` won't advance the ref — forces fresh-clone salvage; explicit `?#<sha>` re-anchors, a bisect lead), GET-018 (atomic wt GET — stage/swap or journal+rollback so a mid-write failure leaves ref/wtlog/tree unchanged; split from GET-016 part 1); GET-013 landed (`a3fc9aed`: scoped `be get <path>` mkdir -p's the vanished parent before checkout-write — `sniff_get_blob_to_wt` mirrors the subtree-overlay path; test/get/34 case D).
 -  [HEAD] — dry-run preview gaps; HEAD-001 done, HEAD-002 landed (`90f4a362`: self-hosted trunk resolves via local `?` tip + coherent GRAFNONE error, test head/23).
 -  BLAME — blame perf on inflate-bound walks; BLAME-004 fixed, BLAME-001/002/003 planned.
 -  [DIFF] — the `diff:` view; DIFF-001 part-a **LANDED `ef0430da`** (`be diff:?<sha>` on a pin-bump commit now renders the gitlink change `<sub> <old40>..<new40>` — the ref-vs-ref tree diff collected only REG/EXE/LNK and dropped WALK_KIND_SUB gitlinks; validated vs the real ABC-002 bump; test/diff/08); part-b open (recurse into the sub for its commit-range content diff, mirroring GET/POST/PATCH; honor `--nosub`) — part-b DEFERRED on a graf↔be design fork; DIFF-002 **LANDED `ac3d389f`** (working-tree `be diff` on a sub-dirty wt routed the sub content past the pager onto the bare terminal — `BERunPipeSubs` folds the sub fan-out into bro's stdin; test/diff/09; dropped dead `BERunPipe`).
 -  [CODE] — code-redundancy cleanup from the 2026-06-11 per-dir review (63 findings, 12 reviewers); CODE-001..015 dir-themed behavior-preserving refactors (reimplements-abc / copy-paste / dead-dup), cleared via [Issues] one store-backed worktree per dir; abc (CODE-001) and load-bearing spawn (CODE-008) gate the diff; CODE-014 **LANDED `f8b04bf3`** (mark abc utf8/eq/suffix + close/inline triplets); CODE-002/003 **LANDED `927d3ec5`** (dog hex/dir/enc→abc via HEXu8sValid/FILEIter, scheme tables→`u8slit` u8cs, ron60/WHIFF/ULOG dup collapse; +DOGTestProjector/WHIFFtest); CODE-004/005 **LANDED `106fff77`** (keeper dedup commit-BFS/tree-descent/cap-parse/obj-hdr; sha1 decode via dog sha1hex, `PACKu8sFeedObjHdr`→dog/git/PACK; broad sha1↔hex sweep tracked as CODE-016); CODE-016 **LANDED `0a43dec5`** (sha1↔hex centralized in dog/git/SHA1.h — `sha1FromHex` decode + 24 raw `{data,+20}`+HEX sites swept; `sha1hex` struct kept as value/key only). CODE-001..015 abandoned (low quality).

[Plan]: ../wiki/Plan.mkd "the live planning page"
[Issues]: ../rule/Issues.mkd "the parallel fix-and-land loop"
[ABC]: ABC/README.mkd "core abc library primitives tickets"
[ULOG]: ULOG/README.mkd "append-log & store durability tickets"
[URI]: URI/README.mkd "branch/commit naming tickets"
[POST]: POST/README.mkd "commit / fast-forward / push tickets"
[PUT]: PUT/README.mkd "reflog-edit / staging tickets"
[GET]: GET/README.mkd "checkout / fetch / view / search tickets"
[HEAD]: HEAD/README.mkd "head dry-run preview tickets"
[DIFF]: DIFF/README.mkd "diff: view tickets"
[CODE]: CODE/README.mkd "code-redundancy cleanup tickets"
