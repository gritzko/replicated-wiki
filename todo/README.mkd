#   Active ticket board

Index of Beagle's open work: one topic per dir, each holding numbered [Plan]-shaped tickets cleared by the parallel [Issues] fix-and-land loop. Each line below names a topic and its live state; per-topic detail lives in `todo/<TOPIC>/README.mkd`. A fully-landed ticket drops from its topic page; a closed topic drops from here.

##  Active topics

 -  [URI] — one keeper rule for branch/commit naming; URI-001 St.1-3 landed (branch-first, length-agnostic, scope-only canonical), St.4-5 pending; URI-002 open (unify bang-factor handling — per-component bang bitflag byte + one `dog/` debanger, replacing the ad-hoc verb-bang→`--force` shim).
 -  DIS — cross-cutting spec/impl discrepancies; DIS-025/002 fixed, DIS-001/024 (store layout), DIS-029 (MARK bare-indent divs), DIS-030+031 landed (`bbefb693`: bang-model patch/post — `!`=whole-branch/forget, `post!`=--force, ban msgs ending in `!`), DIS-032 (specs to bang model, edited on disk); DIS-033 (stale `.refs.idx` → silent-empty `be log:`), DIS-034 (`be put <dir>` needs trailing slash → PUTNONE), DIS-035 (`be patch <scheme>://host?br` remote fetch-merge WIREFAIL, bang-model regression) open; DIS-026/027/028 landed (`fbfea176`/`262d8922`/`465703db`: push cur→git-remote branch, relay peer push errors, `?main` resolves peer's refs/heads/main).
 -  MEM — memory-safety audit, 36 findings; 12 landed 2026-06-07 (suite green), js 007/032/033 blocked on JSC, mediums/lows pending; digest in `MEM/MEM.mkd`.
 -  SUBS — submodule recursion; SUBS-001..012 landed, SUBS-013/015 open; digest in `SUBS/SUBS.mkd`.
 -  [POST] — commit-path gaps; POST-003 landed (`50b8c468`: recursive POST enumerates freshly-added subs, `test/post/15`), POST-004 landed (`50b8c468`: PATCH detached-sub recursion, `test/post/17`), POST-005 landed (dogs merge: untracked mtime-alias not swept, `test/post/28`), POST-002 landed (`2b6026af`: `#`-led msg body → fragment verbatim in `DOGNormalizeArg`, grammar untouched/RFC 3986, `test/post/34`); POST-010 landed (`03355e9b`: swallow benign EPIPE on a no-op `file://` re-push, peer-side `RECVEmitResponse` n==0, `test/post/35`), POST-011 part A landed (`a6129afc`: noop `be patch` stages no patch row, `be-patch-noop`; part B = clearing staged rows → [GET]-008 `be get!`), POST-012 landed (`5686c2b3`: suppress no-op "nothing to promote" line in `-q`/`--tlv` sub recursion, `test/post/36`); open: POST-006 (`POSTCFLCT` false-fires on comment markers — resolution: detect WEAVE shape else `be post!`), POST-007 (`be post` mangles `be://`→`be:/`, rejects multi-line msgs), POST-013 (sub on synthetic `?.beagle/` branch won't push to a git remote without explicit `?main` — should default to remote's main/master); POST-008 landed (`ebde9097`: host-less push routable-target gate), POST-009 landed (`563d7a16`: strip `?/proj` selector before push branch), POST-001 landed (`eabf0c6e`/`8254ee85`: `--nosub` honored + scheme-gated recursion).
 -  [PUT] — staging gaps; PUT-001 landed (`d628fd67`: `be put <sub>/<path>` relays into a mounted sub via `BEActSubsPut`/`BERelaySub`, wtlog-only, `test/put/12`). No open tickets; DIS-034 (`be put <dir>` trailing-slash) tracked under DIS.
 -  [GET] — checkout gaps; GET-001 landed (`3251cb48`: weave-merge BASS BNOROOM), GET-002 landed (`0f7d06b2`: bare-authority URI completion from history), GET-003 landed (`a70e34cd`: `be://host?/proj` empty-path clone past WIRECLFL), GET-005 landed (`b5771abc`: `SNOROOM` large-pack wire ingest), GET-006 landed (`64e42597`: `be get` ancestor-wtlog escape; re-target c->repo at cwd, test/get/40), GET-007 landed (`5124b2ee`: `be://` re-fetch shard bloat — `wire_locate_sha` latest-copy so the watermark stops re-shipping duplicate tails; `WIREREFETCH`/`WIREE2EREFETCH`; deeper reachability rewrite → GET-009); GET-008 landed (`23ad85b6`: verified — `be get! '?'` already clears a poisoned wtlog via its appended `get`-row boundary, append-only, no truncation; test/get/41); open: GET-004 (verify `be://` path addressing — `user@`⇒home, `*.be`=store, else `<path>/.be` dir-or-file), GET-009 (efficient `be://` sync umbrella: reachability upload-pack + cross-file serving + redundant-pack GC; St.1 landed via GET-007).
 -  [HEAD] — dry-run preview gaps; HEAD-001 done, HEAD-002 landed (`90f4a362`: self-hosted trunk resolves via local `?` tip + coherent GRAFNONE error, test head/23).
 -  BLAME — blame perf on inflate-bound walks; BLAME-004 fixed, BLAME-001/002/003 planned.

[Plan]: ../wiki/Plan.mkd "the live planning page"
[Issues]: ../rule/Issues.mkd "the parallel fix-and-land loop"
[URI]: URI/README.mkd "branch/commit naming tickets"
[POST]: POST/README.mkd "commit / fast-forward / push tickets"
[PUT]: PUT/README.mkd "reflog-edit / staging tickets"
[GET]: GET/README.mkd "checkout / fetch / view / search tickets"
[HEAD]: HEAD/README.mkd "head dry-run preview tickets"
