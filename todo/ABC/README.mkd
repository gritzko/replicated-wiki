#   ABC tickets: core library primitives

Discrepancies in `abc/` — the foundational slice/buffer/net/poll/curl layer every other module builds on. Each entry is one ticket with its live state; it drops off once the ticket closes. The fix-and-land method is [Issues]. Memory-safety findings live under [MEM]; this topic is for behavioural/correctness gaps.

##  Active tickets

 -  [ABC-004] — add `abc/BIT.h`: a `u1` bit type + bitmap slice/buffer
    (`u1s`/`u1cs`/`u1b`) + word-parallel set algebra; migrates the ad-hoc
    `BitAt`/`Set`/`Unset`, fixes the broken `BitUnset` (`FILE_RW`), backs
    [DOG-001]'s `weavescope`.

ABC-003 (`UTF8_LEN[byte>>4]` byte-length table shifted; multibyte lead bytes
over-count) landed into the abc sub — `02a23dc3` (`?/abc/.dogs`) + pin `cb71e65b`.
(BRO-004's incoherence was a separate bro bug, not this table — see [[ABC-003]].)

[Issues]: ../../rule/Issues.mkd "the parallel fix-and-land loop"
[MEM]: ../MEM/README.mkd "memory-safety audit tickets"
[ABC-003]: ABC-003.mkd "UTF8_LEN table shifted"
[ABC-004]: ABC-004.mkd "abc/BIT.h — u1 bit type + bitmap set algebra"
[DOG-001]: ../DOG/DOG-001/README.mkd "weavescope adopts u1 as its backing"
[BRO-004]: ../BRO/BRO-004.mkd "colour diff render consumer"
